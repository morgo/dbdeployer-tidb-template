#!/bin/bash
{{.Copyright}}
# Generated by dbdeployer {{.AppVersion}} using {{.TemplateName}} on {{.DateTime}}
source {{.SandboxDir}}/sb_include

# This requires `lsof` which may not be installed.
# May need a preflight check or a fallback.

lsof -Pi :{{.Port}} -sTCP:LISTEN -t >/dev/null 
exit $?
